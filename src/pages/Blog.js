import React from 'react';
import './Blog.css';

function Blog() {
  return (
    <div className="blog">
      <h2>blog</h2>
      <ul>
        <li>"why blockchain isn't a phase, mom"</li>
        <li>"building fintech with less stress (and less coffee)"</li>
        <li>"what happens when you give crypto to gen z?"</li>
      </ul>
    </div>
  );
}

export default Blog;
